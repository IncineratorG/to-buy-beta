import React from 'react';
import {View, StyleSheet} from 'react-native';

const UnitsOption = ({state, unitsList}) => {
  return <View style={styles.mainContainer} />;
};

const styles = StyleSheet.create({
  mainContainer: {
    flex: 1,
    backgroundColor: 'red',
  },
});

export default UnitsOption;
