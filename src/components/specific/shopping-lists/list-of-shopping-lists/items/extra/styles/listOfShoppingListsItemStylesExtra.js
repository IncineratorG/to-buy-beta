import {StyleSheet} from 'react-native';

export const listOfShoppingListsItemStylesExtra = StyleSheet.create({
  mainContainer: {
    backgroundColor: 'transparent',
    height: 90,
    width: 100,
    // marginBottom: 8,
  },
});
