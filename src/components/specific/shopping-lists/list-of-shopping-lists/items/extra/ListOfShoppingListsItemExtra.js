import {View} from 'react-native';
import React from 'react';

const ListOfShoppingListsItemExtra = ({styles}) => {
  return <View style={styles.mainContainer} />;
};

export default React.memo(ListOfShoppingListsItemExtra);
